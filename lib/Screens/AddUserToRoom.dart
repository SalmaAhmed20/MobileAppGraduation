

import 'package:flutter/cupertino.dart';

class AddUserToRoom extends StatelessWidget {
  static const String ROUTE_NAME = 'UserToRoom';
  const AddUserToRoom({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
